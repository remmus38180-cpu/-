# 提案產生器導入信（可直接複製貼上寄出）

> 以下 `主旨`／`本文` 可直接貼進 Outlook。方括號 `〔〕` 內是寄信前要自行替換的內容。

---

## 主旨

藥品支付價提案產生器（單一 HTML 檔）試用說明 — 請於使用前先看「注意事項」

## 附件

1. `case_template.html` — 工具本體（**這一個檔就是全部**）
2. `資料邏輯說明.html` — 規則與計算邏輯說明頁
3. `範例輸出_提案.docx`、`範例輸出_四分頁報表.xlsx`、`範例輸出_提案附件.xlsx` — 假資料樣張

## 本文

各位同仁好：

附件是「藥品支付價提案產生器」，用來把**藥品主檔**與**案件需求檔**兩份 Excel，一次產出提案所需的三份文件，減少手工排版與逐案對數字的時間。已用假資料與模擬主檔驗過，現在想請大家在實際案件上試跑，回饋問題。

### 一、它會產出什麼

| 產出 | 內容 |
|---|---|
| **提案 Word** | 一份需求檔＝一案，多項藥品合併成一份 docx。說明一～八、表一／表二／表五、各藥品的核價試算表都已排好版 |
| **四分頁 Excel 報表** | 藥價核定試算／分組統計／項目明細／項目明細（含 0 元） |
| **提案附件 Excel** | 提案 Word 中 `(P-)` 所指的紙本附件清單，A4 橫向、已設好列印範圍 |

### 二、怎麼開、怎麼用

1. 把 `case_template.html` **存到自己電腦**，用瀏覽器（Edge／Chrome 皆可）**直接開本機檔**。
2. 步驟 01 上傳藥品主檔與案件需求檔（`.xlsx` 或 `.csv`）。
   　沒有需求檔的話，同一區塊有「下載案件需求檔範本」按鈕，範本內附下拉選單與「填表說明」分頁。
3. 步驟 02 填支付價年月（民國 YYYMM，例 115 年 4 月填 `11504`）。
4. 步驟 03 是 Excel 四分頁報表的篩選條件，留空即不篩選（**不影響提案 Word**）。
5. 步驟 04 先勾選「提案附件要產出哪幾張表」，再按「產生預覽」核對，確認無誤後下載三份檔案。

**所有運算都在自己的瀏覽器裡完成，資料不會上傳、不需連外網路。** 檔案可以放在個人電腦離線使用。

### 三、使用注意事項（請務必先看）

**檔案與環境**
- 請用**本機存下來的檔**開啟。從對話或網頁預覽開啟的版本會被沙箱擋住下載，按了沒反應。
- 產檔過程中畫面下方有一塊 log 訊息區，**綠色＝成功、黃色＝提醒、紅色＝失敗**，出問題時請把該區文字一起回報。

**案件需求檔的填法**
- **一份需求檔＝一案**，`CODE` 為必填，**沒有 `CODE` 的列會整列略過**。
- 一項藥品有多筆來文時，把同一個 `CODE` 貼成多列、每列填一筆文號即可；**第 2 列起其他欄位都可以留白**，系統會合併成一項藥品（各欄取第一個非空值），文號則全部保留並自動去重。
- `ATC_MODE` 留空或填不出來時一律視為 `BOTH`（表二會產出「同 ATC7 碼」與「同 ATC 前 5 碼」兩張表）。
- **`十國中位價` 留空或填 0，十國藥價法整欄會印「無」**，只呈現成本法，這是預期行為。

**提案附件的分頁勾選（本次新增）**
- 過去是固定產出六張表，現在改成在步驟 04 自己勾。預設仍是六張全勾，也可以按「只選 Word 會引用的三張」快速切到「同分組＋ATC 前 5 碼＋ATC7 碼」。
- **`ATC 前 1 碼`、`ATC 前 3 碼` 兩張資料量非常大**（前 1 碼等於主檔中該英文字母的全部藥品，動輒數千項、上百頁），定位是**電子查閱**，不建議整張列印。不需要時取消勾選，產檔會更快、檔案更小。

**Word 產出後仍需人工處理的部分**
- 說明三（供應狀況）、說明四（其他廠商回覆）、說明七（專家意見三項）**刻意留白**，請承辦人自行填寫。
- 各處的 `(P-)` 是附件頁碼欄位，請依實際附件頁次手填。
- 「收載超過／未滿 15 年」在無法從主檔判讀時會印「超過／未滿」兩者，請人工圈選。

**數字與既有試算程式的差異**
如果和舊報表對不起來，先確認是不是以下三項（皆為**刻意調整**，非錯誤）：
1. 加成級距改依條文的「**小於等於**」判定，恰好落在 50 萬／100 萬的案件會與舊報表不同。
2. 級距判定金額一律用**整個分組代碼**的總計。
3. 已加上浮點誤差防護，少數金額舊程式會少算一分錢（例：成本 3.8 加成 20%，正確 4.56）。

**主檔欄位缺漏的影響**
- 0 元項目若 `生效日期` 空白或格式異常，**該項目不會做年度歸零**，舊年度資料會整包計入小計。第一次跑真實主檔時，建議先掃一次 0 元項目的生效日期。
- 主檔若缺 `中文名稱`、`必要藥品`、`劑型`，提案附件對應欄位會留空（不會報錯）。

### 四、什麼事該看哪份資料

| 想確認的事 | 看哪裡 |
|---|---|
| 每個欄位怎麼填、有哪些規則 | 案件需求檔範本的**「填表說明」分頁**（工具介面步驟 01 可下載） |
| 計算規則、與既有試算程式的差異、占率為什麼不會加總 100% | **`資料邏輯說明.html`**（離線可開，五個頁籤） |
| 產出的版面長什麼樣、要不要調整格式 | 三個 `範例輸出_*` 樣張 |
| 提案 Word 表一／表二的數字從哪來、想看完整清單 | **提案附件 Excel** 的「同分組」「ATC7 碼」「ATC 前 5 碼」分頁 |
| 核價試算每一格怎麼算出來的 | 四分頁報表的**分頁 1「藥價核定試算」**（含註 1／註 2 與級距說明） |
| 某分組的完整項目（含 0 元、暫停支付項目） | 四分頁報表的**分頁 4「項目明細（含 0 元）」** |
| 分組層級的彙總、跨 ATC 的分組 | 四分頁報表的**分頁 2「總覽篩選_分組統計」** |
| 載入失敗、產檔失敗、數字有疑義 | 畫面下方 **log 訊息區**，連同錯誤訊息回報 |

### 五、回饋

目前尚待用**實際主檔**完整驗證的部分：表二「一個分組跨多個 ATC7 碼」的呈現，以及表一／表二數字與現行報表的核對。麻煩大家試跑時特別留意這兩處。

有任何問題、發現數字有疑義，或希望增加的欄位與表格，請直接回信給我〔或：分機 XXXX〕。

〔署名〕
